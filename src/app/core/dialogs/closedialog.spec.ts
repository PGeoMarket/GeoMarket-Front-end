import { Closedialog } from './closedialog';

describe('Closedialog', () => {
  it('should create an instance', () => {
    const directive = new Closedialog();
    expect(directive).toBeTruthy();
  });
});
